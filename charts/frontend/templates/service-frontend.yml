apiVersion: v1
kind: Service
metadata:
  name: {{Release.name}}-service
  namespace: {{.Values.namespace}}
spec:
  type: LoadBalancer
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP